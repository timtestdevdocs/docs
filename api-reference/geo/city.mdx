---
title: "City"
api: "GET https://api.jigsawstack.com/v1/geo/city"
description: "Get geo information for a city"
---

### Query

<ParamField query="country_code" type="string">
  The country code of the city.
</ParamField>

<ParamField query="state_code" type="string">
  The state code of the city.
</ParamField>

<ParamField query="city_code" type="string">
  The city code of the city.
</ParamField>

<ParamField query="search_value" type="string">
  The search value of the city.
</ParamField>

<ParamField query="lat" type="string">
  The latitude of the city.
</ParamField>

<ParamField query="lng" type="string">
  The longitude of the city.
</ParamField>

<ParamField query="limit" type="string" default="10">
  The limit of the city.
</ParamField>

<Snippet file="header.mdx" />

### Response

<ResponseField name="success" type="boolean">
  Indicates whether the call was successful.
</ResponseField>

<ResponseField name="message" type="string">
  The message returned by the API.
</ResponseField>

<ResponseField name="data" type="JSON">
  Array containing the data returned by the API.
</ResponseField>

<RequestExample>

```javascript Node.js
const endpoint = "https://api.jigsawstack.com/v1/geo/city";
const result = await fetch(endpoint);
const city = await result.json();
```

```bash cURL
curl --location --request GET 'https://api.jigsawstack.com/v1/geo/city' \
--header 'x-api-key: <your-api-key>'
```

</RequestExample>

<ResponseExample>

```json Response
{
  "success": true,
  "message": "Request was successful",
  "city": [
    {
      "state_code": "SGP_SINGAPORE",
      "name": "Singapore",
      "city_code": "SGP_SINGAPORE_SINGAPORE",
      "created_at": "2023-05-19T08:28:48.340558+00:00",
      "updated_at": "2024-01-04T07:38:57.18892+00:00",
      "state": {
        "name": "Singapore",
        "country": {
          "timezones": [
            {
              "abbr": "SGT",
              "name": "Singapore Time",
              "offset": 8,
              "zone_name": "Asia/Singapore",
              "gmt_offset": 28800,
              "gmt_offset_name": "UTC+08:00"
            }
          ]
        }
      }
    }
  ]
}
```

</ResponseExample>
