---
title: "Add Bulk Contacts"
api: "POST https://api.jigsawstack.com/v1/email/contact/bulk"
description: "Add bulk contacts."
---

### Body Parameters

<ParamField body="contacts" type="array<object>" required>
<Expandable title="property" defaultOpen="true">
<ParamField body="email" type="string" required>
The email address of the contact.
</ParamField>
<ParamField body="first_name" type="string">
The first name of the contact.
</ParamField>
<ParamField body="last_name" type="string">The last name of the contact.</ParamField>

<ParamField body="metadata" type="object">
 Set of key-value pairs that you can attach to the contact object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. E.g `{ city: "Singapore" }`
</ParamField>

<ParamField body="audiences" type="array">
  The audiences to add contact to.
  <Expandable title="property" defaultOpen="true">
    <ParamField body="audience_id" type="string">
      The audience id.
    </ParamField>
    <ParamField body="audience_metadata" type="object">
      Set of key-value pairs that you can attach to the contact object. This can be useful for storing additional information about the contact in an audience in a structured format. E.g. `{ ative: true }`
    </ParamField>
  </Expandable>
</ParamField>

</Expandable>
</ParamField>

<Snippet file="header.mdx" />

### Response

<ResponseField name="success" type="boolean">
  Indicates whether the call was successful.
</ResponseField>

<RequestExample>

```javascript Node.js
const endpoint = "https://api.jigsawstack.com/v1/email/contact/bulk";
const options = {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    "x-api-key": "<your-api-key>", // Replace with your actual API key.
  },
  body: JSON.stringify({
    contacts: [
      {
        email: "john-doe@example.com",
      },
      {
        email: "john-doe@example.com",
      },
    ],
  }),
};

const result = await fetch(endpoint, options);
const data = await result.json();
```

```bash cURL
    curl --location --request POST 'https://api.jigsawstack.com/v1/email/contact/bulk' \
    --header 'Content-Type: application/json' \
    --header "x-api-key: <your-api-here>" \
    --data '{
           contacts: [
      {
          email: "john-doe@example.com",
      },
      {
       email: "john-doe@example.com",
      },
    ],
    }'
```

</RequestExample>

<ResponseExample>

```json Response
{
  "success": true
}
```

</ResponseExample>
