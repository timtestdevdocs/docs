---
title: "Get List of Contacts"
api: "POST https://api.jigsawstack.com/v1/email/contact/list"
description: "Get contacts."
---

### Query

<ParamField query="limit" default="30" type="string">
  The number of contacts to return.
</ParamField>

<ParamField query="page" default="1" type="string">
  The page to return.
</ParamField>

<ParamField body="audience" type="object">
  <Expandable title="property">
    <ParamField body="ids" type="array<string>">
      The audience ids to filter by.
    </ParamField>
    <ParamField
      body="filter_subscribed"
      type="string"
      default="all"
    >
  Supported values are:
     <ul>
       <li>`all`</li>
       <li>`subscribed`</li>
       <li>`unsubscribed`</li>
     </ul>

    </ParamField>

  </Expandable>
</ParamField>

<ParamField query="search_value" type="string">
  Retrieve contacts that are similar to the given email search value.
</ParamField>

<Snippet file="header.mdx" />

### Response

<ResponseField name="success" type="boolean">
  Indicates whether the call was successful.
</ResponseField>

<RequestExample>

```javascript Node.js
const endpoint = "https://api.jigsawstack.com/v1/email/contact/list";
const options = {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    "x-api-key": "<your-api-key>", // Replace with your actual API key.
  },
  body: JSON.stringify({
    page: "2",
  }),
};
const result = await fetch(endpoint, options);
const data = await result.json();
```

```bash cURL
    curl --location --request POST 'https://api.jigsawstack.com/v1/email/contact/list' \
    --header 'Content-Type: application/json' \
    --header "x-api-key: <your-api-here>" \
    --data '{
      page:"2"
    }'
```

</RequestExample>

<ResponseExample>

```json Response
{
  "success": true,
  "list": [
    {
      "email": "john-doe@example.com",
      "first_name": "John",
      "metadata": null,
      "last_name": null
    }
  ],
  "page": 0,
  "limit": 10,
  "has_more": false
}
```

</ResponseExample>
