---
title: "Add"
api: "POST https://api.jigsawstack.com/v1/store/simplekv"
description: "Add data to KV store."
---

### Body

<ParamField body="key" type="string" required>
  The key to store the data under.
</ParamField>

<ParamField body="value" type="string" required>
  The value to store.
</ParamField>

<ParamField body="encrypt" type="boolean">
  Whether to encrypt the data.
</ParamField>

### Header

<Snippet file="header.mdx" />

### Response

<ResponseField name="success" type="boolean">
  Indicates whether the call was successful.
</ResponseField>

<RequestExample>

```javascript Node.js
const endpoint = "https://api.jigsawstack.com/v1/store/simplekv";
const options = {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    "x-api-key": "<your-api-key>", // Replace with your actual API key.
  },
  body: JSON.stringify({
    key: "pisa",
    value: "Leaning Tower",
  }),
};
const result = await fetch(endpoint, options);
const data = await result.json();
```

```bash cURL
curl --location --request POST 'https://api.jigsawstack.com/v1/store/simplekv' \
--header 'Content-Type: application/json' \
--header 'x-api-key: <your-api-key>' \
```

</RequestExample>

<ResponseExample>

```json Response
{
  "success": true
}
```

</ResponseExample>
