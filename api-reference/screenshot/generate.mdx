---
title: "Screenshot"
api: "POST https://api.jigsawstack.com/v1/screenshot"
description: "Takes a screenshot of a website."
---

### Body

<ParamField body="url" type="string" required>
  The URL of the website to take a screenshot of.
</ParamField>

<ParamField body="full_page" type="boolean">
  Whether to take a full page screenshot.
</ParamField>

<ParamField body="omit_background" type="boolean">
  Whether to omit the background.
</ParamField>

<ParamField body="quality" type="number" default="75">
  The quality of the screenshot. 0-100.
</ParamField>

<ParamField body="type" type="string" default="png">
  The type of the screenshot. Can be either webp, jpeg or png.
  <ul>
    <li>jpeg</li>
    <li>png</li>
    <li>webp</li>
  </ul>
</ParamField>

<ParamField body="width" type="number" default="1920">
  The width of the screenshot.
</ParamField>

<ParamField body="height" type="number" default="1080">
  The height of the screenshot.
</ParamField>

<ParamField body="screen_size_preset" type="string">
  The screen size preset to use.
</ParamField>

<Snippet file="header.mdx" />

### Screen size presets

<ul>
  <li>`QVGA`</li>
  <li>`VGA`</li>
  <li>`SVGA`</li>
  <li>`HD`</li>
  <li>`HD+`</li>
  <li>`SXGA`</li>
  <li>`FHD`</li>
  <li>`2K`</li>
  <li>`2K QHD`</li>
  <li>`5K`</li>
  <li>`4K UHD`</li>
</ul>

See the full list of screen size presets [here](https://gist.github.com/iamNarcisse/c48d72301de1c12f722cf2eb175fc4bc).

### Response

<ResponseField name="success" type="boolean">
  Indicates whether the call was successful.
</ResponseField>

<RequestExample>

```bash cURL
curl --location --request POST 'https://api.jigsawstack.com/v1/screenshot' \
--header 'Content-Type: application/json' \
--header 'x-api-key: <your-api-key>' \
--data '{
    "url": "https://jigsawstack.com"
}'
```

```javascript Node.js
const endpoint = "https://api.jigsawstack.com/v1/screenshot";
const options = {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    "x-api-key": "<your-api-key>", // Replace with your actual API key.
  },
  body: JSON.stringify({
    url: "https://jigsawstack.com",
  }),
};
const result = await fetch(endpoint, options);
const data = result.json();
```

</RequestExample>

<ResponseExample>

```json Response
{
  "success": true
}
```

</ResponseExample>
