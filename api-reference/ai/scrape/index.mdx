---
title: "AI Scraper"
api: "POST https://api.jigsawstack.com/v1/ai/scrape"
description: "This endpoint allows you smartly scrape a web page using prompts."
---

### Body

<ParamField body="url" type="string" required>
  URL of the page to scrape
</ParamField>

<ParamField body="element_prompts" type="array<string>" required>
  Items on the page to scrape. E.g. Plan price, Plan title
</ParamField>

<Snippet file="header.mdx" />

### Response

<ResponseField name="success" type="boolean">
  Indicates whether the call was successful.
</ResponseField>

<ResponseField name="data" type="json">
  Scrape data of the page
</ResponseField>

<RequestExample>

```bash cURL
curl --location --request POST 'https://api.jigsawstack.com/v1/ai/scrape' \
--header 'Content-Type: application/json' \
--header 'x-api-key: <your-api-key>' \
--data '{
    url:"https://supabase.com/pricing",
    element_prompts: ["Plan header", "Plan price"]
}'
```

```javascript Node.js
const endpoint = "https://api.jigsawstack.com/v1/ai/scrape";
const options = {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    "x-api-key": "<your-api-key>", // Replace with your actual API key.
  },
  body: JSON.stringify({
    url: "https://supabase.com/pricing",
    element_prompts: ["Plan title", "Plan price"],
  }),
};
const result = await fetch(endpoint, options);
const data = result.json();
```

</RequestExample>

<ResponseExample>

```json Response
{
  "success": true,
  "data": {}
}
```

</ResponseExample>
