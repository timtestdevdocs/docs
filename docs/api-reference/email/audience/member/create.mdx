---
title: "Add Member"
api: "POST https://api.jigsawstack.com/v1/email/audience/{audience_id}/member"
description: "Add a contact to an audience."
---

### Path Parameters

<ParamField path="audience_id" type="string" required>
  The audience id.
</ParamField>

### Body Parameters

<ParamField body="email" type="string" required>
  The email address of the contact.
</ParamField>

<ParamField body="metadata" type="object">
 Set of key-value pairs that you can attach to the contact object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. E.g `{city:"Singapore"}`
</ParamField>

<Snippet file="header.mdx" />

### Response

<ResponseField name="success" type="boolean">
  Indicates whether the call was successful.
</ResponseField>

<RequestExample>

```javascript Node.js
const endpoint =
  "https://api.jigsawstack.com/v1/email/audience/{audience_id}/member";
const options = {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    "x-api-key": "<your-api-key>", // Replace with your actual API key.
  },
  body: JSON.stringify({
    email: "john-doe@example.com",
    metadata: { city: "Singapore" },
  }),
};
const result = await fetch(endpoint, options);
const data = await result.json();
```

```bash cURL
    curl --location --request POST 'https://api.jigsawstack.com/v1/email/audience/{audience_id}/member' \
    --header 'Content-Type: application/json' \
    --header "x-api-key: <your-api-here>" \
    --data '{
    "email": "john-doe@example.com",
      metadata: { city: "Singapore" },
    }'
```

</RequestExample>

<ResponseExample>

```json Response
{
  "success": true,
  "id": "1773978e-409f-41d5-8c0e-18fe6220e496"
}
```

</ResponseExample>
